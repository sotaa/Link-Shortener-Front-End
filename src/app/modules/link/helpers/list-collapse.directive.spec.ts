import { ListCollapseDirective } from './list-collapse.directive';

describe('ListCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new ListCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
